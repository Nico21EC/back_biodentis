/*const mongoose2=require('mongoose');
const esquema=require('../model/login/Odontologo');

esquema.statics={
    createOdontolo:function(data,cb){
       
        const user= new this(data);
        user.save(cb);
    },
    loginOdontolo:function(query,cb){
        this.find(query,cb);

    }
}
const authModel=mongoose2.model('Odontologo',esquema);
module.exports=authModel;*/